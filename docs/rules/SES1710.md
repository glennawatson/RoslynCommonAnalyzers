# SES1710 — Do not disable antiforgery validation on a form

**Category:** Blazor
**Severity:** Warning
**Code fix:** None — see [Why there is no code fix](#why-there-is-no-code-fix)

Antiforgery (anti-CSRF) validation checks that a state-changing form post carries a
token the server issued to the same user, which is what stops a malicious page from
silently submitting a request on the victim's behalf. A form or component is
protected by default; passing `required: false` to the `[RequireAntiforgeryToken]`
attribute turns that check off, so a forged cross-site post is accepted and
processed.

```razor
@* reported (SES1710): antiforgery is switched off for this form's posts *@
@attribute [RequireAntiforgeryToken(required: false)]
```

Remove the argument (or pass `true`) so the antiforgery token is validated:

```razor
@* protected: the token is required, the default *@
@attribute [RequireAntiforgeryToken]
```

## What is reported

An application of
`Microsoft.AspNetCore.Antiforgery.RequireAntiforgeryTokenAttribute` — on a
component or a method — is flagged when its `required` argument is the constant
`false`, in either the named (`required: false`) or positional (`false`) form. The
attribute constructor is bound and its containing type confirmed, so a same-named
attribute from another namespace is ignored. The attribute with no argument keeps
its protective default (`required` is `true`) and is not reported.

## Why there is no code fix

Deleting the argument re-enables validation, but disabling antiforgery is sometimes
deliberate — a stateless, session-free endpoint that changes nothing — and the
analyzer cannot tell an intentional exception from a mistake. The rule reports and
leaves the decision to the developer; disable it only for such an endpoint, and
never for a public app.

## Proving the API exists

The rule resolves
`Microsoft.AspNetCore.Antiforgery.RequireAntiforgeryTokenAttribute` in the analyzed
compilation and registers nothing when it is absent, so a project without antiforgery
receives no diagnostic and pays no analysis cost.

## Performance

The no-diagnostic path is syntactic. An attribute whose name is not spelled
`RequireAntiforgeryToken` (or `RequireAntiforgeryTokenAttribute`), or that carries no
argument, is rejected before the semantic model is touched; only a matching shape is
bound to confirm the attribute type and read the `required` argument's constant
value.

## See also

- [docs/PERFORMANCE.md](../PERFORMANCE.md)
