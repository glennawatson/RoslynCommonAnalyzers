# SST1621 — Type parameter documentation should declare the type parameter name

| Item | Value |
| --- | --- |
| Category | Documentation |
| Severity | Warning |
| Code fix | No |

## Cause

A `<typeparam>` element has no `name` attribute.

## Rule description

Each `<typeparam>` declares the type parameter it documents via `name="..."`.
