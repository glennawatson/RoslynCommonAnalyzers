# SST1630 — Documentation text should contain whitespace

| Item | Value |
| --- | --- |
| Category | Documentation |
| Severity | Warning (disabled by default) |
| Code fix | No |

## Cause

A documentation summary is a single word with no whitespace.

## Rule description

Off by default, matching the analyzer. Enable it to require summaries to contain more than a
single word.

The StyleSharp counterpart of the analyzer's `the rule`.

## How to fix violations

Expand the summary into a descriptive phrase.
