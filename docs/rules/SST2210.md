# SST2210 - Remove repeated nullable state

Nullable directives should not repeat the same file-local state without an intervening change.

## Example

```csharp
#nullable enable
#nullable enable
```

Use:

```csharp
#nullable enable
```

## Notes

The analyzer scans directives in source order and compares the active file-local directive state. The
code fix removes the repeated directive line.
