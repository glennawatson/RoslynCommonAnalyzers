# SST2211 - Remove nullable restore with no local effect

A `#nullable restore` directive before any file-local nullable state change has nothing to restore.

## Example

```csharp
#nullable restore
```

Use:

```csharp
```

## Notes

The analyzer only reports an early restore before the file has used `#nullable enable` or
`#nullable disable`. The code fix removes the directive line.
