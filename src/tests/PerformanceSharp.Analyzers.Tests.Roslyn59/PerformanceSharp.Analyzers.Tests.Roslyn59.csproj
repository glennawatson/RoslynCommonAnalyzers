<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- The same tests, run against the analyzers built for roslyn5.9. One target framework rather than
         the baseline project's two: what varies across the slots is the compiler the rules bind to,
         not the runtime they execute on, and the baseline already covers both runtimes. -->
    <TargetFramework>net11.0</TargetFramework>
    <RootNamespace>PerformanceSharp.Analyzers.Tests</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.Analyzer.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.CodeFix.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.CodeRefactoring.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic.Analyzer.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic.CodeFix.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic.CodeRefactoring.Testing" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\PerformanceSharp.Analyzers.CodeFixes.Roslyn59\PerformanceSharp.Analyzers.CodeFixes.Roslyn59.csproj" />
    <ProjectReference Include="..\..\PerformanceSharp.Analyzers.Roslyn59\PerformanceSharp.Analyzers.Roslyn59.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="RoslynCommon.Analyzers" />
    <Using Include="RoslynCommon.Analyzers.CodeFixes" />
  </ItemGroup>

  <!-- No test source of its own: it recompiles the baseline project's files against the other slot. -->
  <ItemGroup>
    <Compile Include="..\PerformanceSharp.Analyzers.Tests\**\*.cs"
             Exclude="..\PerformanceSharp.Analyzers.Tests\bin\**\*.cs;..\PerformanceSharp.Analyzers.Tests\obj\**\*.cs"
             LinkBase="PerformanceSharp.Analyzers.Tests" />
    <Compile Include="..\..\Shared\Tests\*.cs" Link="Verifiers\Shared\%(Filename).cs" />
  </ItemGroup>

</Project>
