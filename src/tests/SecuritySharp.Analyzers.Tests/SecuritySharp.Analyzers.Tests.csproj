<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFrameworks>net10.0;net11.0</TargetFrameworks>
    <RootNamespace>SecuritySharp.Analyzers.Tests</RootNamespace>

    <!-- Run the analyzer tests against the newest Roslyn we target (5.9) so the test
         compiler can parse the latest C# syntax. The analyzer source is
         version-agnostic (netstandard2.0), so this exercises the same logic. -->
    <MicrosoftCodeAnalysisVersion>5.9.0</MicrosoftCodeAnalysisVersion>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.Analyzer.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.CodeFix.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.CodeRefactoring.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic.Analyzer.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic.CodeFix.Testing" />
    <PackageReference Include="Microsoft.CodeAnalysis.VisualBasic.CodeRefactoring.Testing" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\SecuritySharp.Analyzers.CodeFixes\SecuritySharp.Analyzers.CodeFixes.csproj" />
    <ProjectReference Include="..\..\SecuritySharp.Analyzers\SecuritySharp.Analyzers.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="RoslynCommon.Analyzers" />
    <Using Include="RoslynCommon.Analyzers.CodeFixes" />
  </ItemGroup>

  <ItemGroup>
    <Compile Include="..\..\Shared\Tests\*.cs" Link="Verifiers\Shared\%(Filename).cs" />
  </ItemGroup>

</Project>
